@* Rendered in place of a content item the current request context must not see.

   Deliberately empty. Returning a shape with a name rather than nothing at all keeps the reason
   visible in shape tracing: the item was resolved and excluded, not missing. *@
